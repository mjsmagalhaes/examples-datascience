{
  "name": "templates",
  "version": "0.0.1",
  "description": "dsapps.wordcloud templates",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "parcel serve ./src/**/*.html --public-url /assets",
    "build": "parcel build ./src/**/*.html --public-url /assets"
  },
  "author": "",
  "license": "ISC",
  "targets": {
    "default": {
      "context": "browser",
      "publicUrl": "/assets"
    }
  },
  "devDependencies": {
    "parcel": "^2.3.2"
  },
  "dependencies": {
    "@parcel/transformer-sass": "^2.3.2",
    "@popperjs/core": "^2.11.2",
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.8.1"
  }
}